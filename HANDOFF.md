# ArcCreate 本家互換開発ハンドオフ

最終更新: 2026-09-06

## 開発目的

このフォークを、AFF譜面の編集機能を維持しながら、同じAFF・再生時刻・入力列に対するノーツ位置、アーク形状、表示状態、入力判定、コンボ、スコアを本家Arcaeaへ近づけた派生版として開発する。

本家バイナリのコードを移植することが目的ではない。所有するAPKを読み取り専用で静的解析し、外部から観測できる入力と出力の対応を独立した仕様・テストとして再実装する。

## リポジトリ状態

- ローカル: `D:\chaos\MAGATU_EMULATOR\ArcCreate`
- リモート: `https://github.com/mansukoxamel/ArcCreate-Next.git`
- 基準ブランチ: `main`
- 移転時の起点: `755c2e8818dab3c8404b3ff997e87373607891cd`（元の`origin/trunk`）
- Unity: `6000.3.20f1`（Unity 6.3 LTS）
- Git署名: `jinn / jinn@local`

`main`を復元可能な基準として保ち、試行錯誤する変更は目的別ブランチで行う。`master`は使わない。

## 解析資料

解析資料の本拠地は[Documentation/ArcaeaResearch](Documentation/ArcaeaResearch)へ移した。以後、ArcCreate本家互換開発に関する調査結果はここを更新する。旧配置のMAGATU TEMPO FORGE側には実体を残さず、移転先への案内だけを残す。

最初に次の順で読む。

1. [ARCAEA_COMPATIBILITY_RESEARCH.md](Documentation/ArcaeaResearch/ARCAEA_COMPATIBILITY_RESEARCH.md) — 本家、ArcCreate、Arcade Plusの確定差分と実装優先順
2. [ARCAEA_NATIVE_ANALYSIS.md](Documentation/ArcaeaResearch/ARCAEA_NATIVE_ANALYSIS.md) — 本家3.6.1／5.5.8の入力判定、判定点、再取得、ネイティブ関数根拠
3. [AFF_NOTE_JUDGEMENT_RESEARCH.md](Documentation/ArcaeaResearch/AFF_NOTE_JUDGEMENT_RESEARCH.md) — ノート型、判定点数、実物譜面との照合
4. [AFF_FORMAT_RESEARCH.md](Documentation/ArcaeaResearch/AFF_FORMAT_RESEARCH.md) — 正式AFF表記と各エディター独自拡張の区別

保存サイト、集計JSON、再現スクリプト、回帰試験も同じディレクトリ内に元の相対構造を保って移した。資料内リンクはそのまま利用できる。

## 外部に置く原資料

次は容量、権利、誤push防止のためGitリポジトリへ複製しない。

- 実物AFF 1,478譜面: `D:\work\arcaea\songs`
- Arcaea 3.6.1: `D:\work\arcaea\APK\3.6.1.mod`
- Arcaea 4.0.255: `D:\work\arcaea\APK\4.0.255.mod`
- Arcaea 5.5.8: `D:\work\arcaea\APK\5.5.8.mod`

解析対象`libcocos2dcpp.so`のSHA-256は`ARCAEA_NATIVE_ANALYSIS.md`に固定してある。バイナリを取り違えない。

## 現在までに確定した重要事項

- ArcCreateはArcade Plusより入力判定と編集機能が発展しており、開発母体に適する。
- Arcade Plusはcameraの`s`とアーク追従tiltなど、一部が本家3.6.1に近い。比較資料として使う。
- アーク8種類`b / s / si / so / sisi / siso / sosi / soso`の連続補間式とX/Y割当はArcCreate、Arcade Plusとも本家3.6.1に一致する。
- アーク描画用の折れ線分割と端点整数化は、0.3.2で本家準拠へ修正した。
- 移転起点のArcCreateではcamera命令`s`がsmoothstepだが、本家3.6.1とArcade Plusは線形である。0.1.1で本家準拠の線形補間へ修正した。
- 本家3.6.1のアーク追従tiltは追従中4%、中央復帰中2%を1更新ごとに補間する。移転起点にあった常時`6 * deltaTime`は、0.1.2で本家準拠の二係数へ修正した。
- 移転起点のArcCreateではZ距離を基準BPMで正規化していたが、0.5.0でtiming区間の積分値とハイスピードから本家準拠の距離へ変換するよう修正した。
- 移転起点のArcCreateにあった厳しすぎるアーク接続条件は、0.6.0で本家3.6.1の時刻・X・Y・線種条件へ修正した。
- 初回取得と保持の入力範囲、物理リリース後の再取得ロック、判定点ごとのMiss猶予、異色アーク近接時の全指受理は、0.7.0で本家3.6.1準拠へ修正した。
- tap、arctap、arcの表示開始時の透明度と、hold／arcの判定面クリップは、0.8.0で本家のネイティブ処理に合わせた。
- 3.6.1で確定し実装へ反映した判定点、接続、入力状態、表示更新の主要経路は、5.5.8のRTTI、仮想関数表、命令列、定数へ対応付けた。
- Arcade Plusには本家相当の手入力判定状態機械がないため、Arcade Plus自体を開発母体にはしない。

数値、式、関数アドレス、反例は必ず解析資料本文を参照する。この要約だけから実装しない。

## 実装方針

見た目を目視だけで合わせると、Z距離とカメラの誤差が互いを隠す。次の順で、純粋関数または小さい状態遷移として試験を先に固定する。

1. camera命令`s`の線形補間（0.1.1で対応済み）
2. アーク追従tiltの追従・復帰係数（0.1.2で対応済み）
3. アークの正規化時間による折れ線分割と端点整数化（0.3.2で対応済み）
4. timing区間をまたぐZ距離積分とハイスピード（0.5.0で対応済み）
5. アーク接続条件（0.6.0で対応済み）
6. 初回取得、保持、離脱、再取得ロック、異色近接猶予（0.7.0で対応済み）
7. tap、hold、arc、arctapの表示開始、消滅、手前クリップ（0.8.0で対応済み）
8. 3.6.1で確定した処理を5.5.8へ命令列・定数・呼出関係で対応付ける（0.8.1で入力平面範囲と異色交差猶予まで対応済み）

本家互換挙動を既定動作とし、切替可能な互換モードは設けない（2026-09-05決定）。本家との差が外部観測と解析で確定した箇所は、根拠と回帰試験を残しながら順次置き換える。AFF編集機能と操作性の改善は維持・発展させる。

## 曲ライブラリ方式（段階的実装中）

- 曲ライブラリの絶対パスやドライブは決め打ちしない。投入されたAFF、OGG、JPGまたはフォルダから曲フォルダを求め、そのフォルダ名を曲ID、親フォルダをライブラリルートとして扱う。
- ライブラリルート直下の本家`songlist`とArcCreate Next追加分`songlist.arcnext.json`を相対位置から探す。本家`songlist`は読み取り専用とし、ArcCreate Nextは変更しない。
- 両方のリストに曲IDがない場合はメタデータを表示せず、従来の直接編集とファイル名による解決を継続する。
- 0.10.0で上記の相対探索と読み取り専用参照を実装済み。タイトル、作曲者、BPM、試聴区間、譜面制作者、ジャケット制作者、難易度、`side`を直接編集用データへ反映する。
- `songlist.arcnext.json`も本家`songlist`と同じ`{"songs": [...]}`構造および本家フィールドを使用する。従来の`.arcproj`固有情報は原則として移行しない。
- 新規曲の`idx`は、読み込んだ本家と追加分にある最大値の続き番号を割り当てる。現行本体が`idx`を参照しない場合でも、将来互換性のため省略しない。
- 本家向け統合出力の前に`idx`重複を必ず検査する。重複を黙って出力せず対象曲を表示し、本家側を変更せずArcCreate Next追加分だけを続き番号へ振り直せるようにする。

> [!WARNING]
> 本家`songlist`と`songlist.arcnext.json`に同じ曲IDがある場合の統合・上書き規則は未決定。初期実装ではどちらも変更せず、衝突した曲IDを利用者へ通知する。勝手な優先順位や自動上書きを実装しない。実装箇所にも同じ内容のTODOコメントを残す。

難易度表示は数値ではなく本家名称を使用する。

| `ratingClass` | 略称 | 表示名 | 状態 |
| --- | --- | --- | --- |
| 0 | PST | Past | 確定 |
| 1 | PRS | Present | 確定 |
| 2 | FTR | Future | 確定 |
| 3 | BYD | Beyond | 確定 |
| 4 | ETR | Eternal | 確定 |
| 未確定 | INS | Inscribed | Ver.7.0で追加。`ratingClass`はVer.7系の実物`songlist`入手後に確定する |

最低限、曲IDに対応する`title_localized`、`artist`、各`difficulties`の名称とレベルを表示する。`side`は0をLight、1をConflict、2をColorlessとして扱う。`bg`および難易度側の`bg`は読み取れる構造を維持するが、0.10.0では実在ファイルを推測せず、`side`の標準背景を使用する。

背景情報と背景画像は必須にしない。`bg`がない、または対応画像が見つからない場合もエラーを表示せず、`side`に応じたLight、Conflict、Colorlessの標準背景へフォールバックし、譜面、音源、曲情報の読み込みを継続する。

## これからやること

本家再現の主要部分は実用上かなり揃ったため、今後は制作から本体導入までの動線と日常的な使い勝手を優先する。次の順序は現時点の候補であり、実際の着手順と細部は利用者の指示を受けて決める。勝手に一括実装しない。

### 1. `songlist.arcnext.json`の作成・更新

- 本家`songlist`は引き続き読み取り専用とし、ArcCreate Nextから一切変更しない。
- 本家・追加用リストに未登録の曲を編集・保存する場合、既存の曲情報入力欄から`title_localized`、`artist`、難易度、レベル、属性など本家方式に存在する情報を追加用`songlist.arcnext.json`へ登録できるようにする。
- 新規曲の`idx`は、本家と追加用リストに存在する最大値の続き番号を割り当てる。
- 既存の追加用エントリを編集した場合は、対象曲だけを更新し、他の曲や未知のフィールドを壊さない。
- 本家と追加用リストの曲ID衝突時の統合・上書き規則は未決定のままである。方針が決まるまでは自動更新せず、衝突を明示する。
- この機能の完成を、従来の曲単位`.arcproj`を曲ライブラリ方式へ置き換える主要段階とする。

### 2. 本体導入前チェック

本体へ曲を持ち込む前に、少なくとも次をまとめて検査できるようにする。異常を黙って補正せず、対象ファイルまたは曲IDと理由を表示する。

- 必要なAFF、OGG、JPEGの不足
- 曲フォルダ名とsonglist曲IDの不一致
- 本家・追加用リスト内の`idx`重複
- 難易度、レベル、属性などの不正値
- AFFの構文エラーおよび読み込み不能
- 本家`songlist`と`songlist.arcnext.json`の曲ID衝突

将来、本家向け統合出力を実装する場合も、この検査を通過条件とする。本家側を自動変更しない。

### 3. 保存状態の明示とクラッシュ復旧

- 編集画面に「未保存」「保存済み」と最終保存時刻または最終自動保存時刻を分かりやすく表示する。
- 異常終了後に通常保存より新しい自動保存またはバックアップが存在する場合だけ、次回起動時に復旧候補を提示する。
- 復旧は利用者が選択して実行し、元のAFFを黙って上書きしない。
- 復旧元、復旧先、時刻を明示し、不要になったバックアップの削除を復旧処理へ混ぜない。

### 4. 編集操作の小規模な改善

実際の利用中に生じた不満を優先し、必要なものだけを個別に実装する候補とする。

- 現在時刻のミリ秒表示は残したまま、その近くに現在の小節番号・拍位置とノーツ進行数を併記する。表示例は「42小節 / 3拍」「417 / 1234ノーツ」のように、譜面作業中に一目で分かる短い形式とする。
- ノーツ数はAFFイベントの個数ではなく、本家のリザルトTOTALおよび攻略情報と対応するスコア判定数を使う。TapとArcTapは各1、Holdと判定ありArcは内部判定点の重みを加算し、`noinput`やトレースなど配点対象外を誤算入しない。
- 現在ノーツ数は、現在の譜面時刻までに到達したスコア判定数として表示する。同時押しや同一時刻へ複数回分が束ねられた箇所では、その時刻を越えた時点で重み分をまとめて進め、任意の順序を捏造しない。
- 初期実装ではノーツ番号を指定した直接移動までは行わない。前の拍・次の拍へ移動する操作を用意し、必要であればキーボードショートカットも割り当てる。
- 小節・拍表示と拍移動は別々の概算を使わず、基準タイミンググループの`timing`イベント、BPM、`divisor`から生成する同じ拍・小節境界を参照する。途中のBPM・拍子変更を考慮する。
- 小節・拍・ノーツ表示は、音源の再生位置ではなくAFF上のノーツと対応する譜面時刻を基準とする。負の時刻、BPM 0区間、基準以外のタイミンググループを編集中の場合の扱いは、実装前に仕様を確認する。
- 譜面切替のキーボードショートカット
- 読み込み履歴のピン留め
- 現在の曲フォルダをすぐ開くボタン
- 選択ノーツの時刻や座標をまとめて数値変更する操作
- 使用頻度の高い操作だけに絞った右クリックメニュー
- プレイ画面のフォントを表示用途ごとに選択できるようにする。対象は大きなスコア数字の`ScoreText`、コンボ数の`ComboText`、曲名の`TitleText`の3系統とし、それぞれ独立した設定として保存する。小さな`SCORE:`見出しの`ScoreLabel`は変更せず、`ComposerText`と`DifficultyText`も対象に含めない。

機能数を増やすこと自体を目的にせず、既存ショートカットや既存UIとの重複を確認してから追加する。

### 5. 特殊譜面の互換調査

- `noinput`
- `designant`
- BPM 0区間のロング／アーク
- `allowsMemes`を含む入力平面への座標変換
- 譜面改訂または特殊規則が疑われる公称ノーツ数不一致

通常の譜面制作への影響が小さいため、現時点では上記1～4より優先度を下げる。推測で本家仕様を決めず、原資料と本家バイナリから挙動を確定できた項目だけを実装する。

### 6. ミス記録（0.10.5～0.10.6で追加）の残り作業

Xynapse（本家練習用MOD）が書き出すミス記録（`miss_*.txt`）をドロップすると、ミスしたノーツを色付けする機能は0.10.6で入れた（赤=LOST、黄=FAR。タイムラインにも縦線。Alt+N／Alt+Pで移動）。実物の初回確認で「十分使える」との評価。以下は、ユーザーの指示（2026-09-22）による今後の修正で、着手順は利用者の指示を受けて決める。

1. **【重要】ミス記録の難易度と、開いている譜面が違う場合は、ミス記録の難易度の譜面を自動で読み込む。** 今は「注意」を出すだけで、開いている譜面（別の難易度など）にそのまま重ねてしまい、ノーツが見つからない。ミス記録の曲ID・難易度（`difficulty:` 行、`miss_<曲ID>_<難易度>_…`）から、同じ曲フォルダの対応する`<難易度番号>.aff`（PAST=0、PRESENT=1、FUTURE=2、BEYOND=3、ETERNAL=4）を探して開いてから重ねる。
   - 【ユーザーの回答、2026-09-22】難易度違いは**自動で読み込む**。**曲が違う場合は、曲の場所を探す**機能を入れるかもしれない。そのために、**曲フォルダの場所を決め打ちで設定**する必要があるかもしれない（既存の曲ライブラリ方式の「ライブラリルート」との関係も含めて、具体的な実装方法は**利用者が後日考える**。未決定のまま実装しない）。
2. ~~ミス間の移動を目立たせる（宣伝する）~~ **【取り下げ、2026-09-22】** ユーザーの言葉は音声入力の変換間違いだった。ショートカット（Alt+N／Alt+P）で移動できる今の形でよい。画面上の案内やボタンは、今のところ要望なし（あっても構わないが、優先しない）。
3. **色の見え方の調整。** 赤・黄は今のままでよい箇所もあるが、**青いアークが、ミス色で描かれると黒っぽくなり、青だと分からない**。青と分かる濃い青（または、黒に見えない色）にする。アークの色は、ミスの色を上書きする方式のため、アークだけ別の扱い（色相を保ったまま暗くする、明るい縁取りにする等）を検討する。ユーザーが後日に考える。

4. **ホールド・アークの「どの粒からミスしたか」を、正確に表示する（難しいが、できなくはない）。** 今は、ミスしたホールド・アークの**全体**が赤くなり、タイムラインは**ノーツの先頭の位置**にしか線が出ない。ミス記録の`judged_ms`列には、**ミスした粒（判定点）ごとの判定時刻**が入っているので、次のように直す。
   - **タイムライン**: ノーツの先頭ではなく、ミスした粒の時刻（`judged_ms`）に線を出す。
   - **ゲーム画面（ホールド）**: 押せていた前半は通常の色のまま、**ミスした粒から先だけ**を赤くする。ホールドは1枚の四角形を引き伸ばして描いているので、ミスした粒の位置で2枚に分けて（前半は通常色、後半は赤）描く案がある。途中から色を隠す、という見せ方も選べる。
   - **ゲーム画面（アーク）**: アークは区間（セグメント）ごとに描いているので、ミスした粒に当たる区間から先を赤くする。
   - 「1粒目はOK、2・3粒目をミス」と「1粒目からミス」が、見た目で区別できるようにする。
   - ミスしていないノーツまで赤くなっているように見える件（ユーザーの指摘）も、この作業の中で精査する。ミス記録との照合（ノーツの特定）が、同時押しやアークで別のノーツを選んでいないか、実物のミス記録で確かめる。
   - アークタップは1点のノーツなので、今の全体色付けのままでよいと考えられる（要確認）。

### 7. ミス記録の集計（新規の要望、2026-09-22。設計前）

ユーザーが練習で使ってみて、「どこでミスするか、傾向が分かってきた。左が弱いらしい」との報告。ミス記録（`miss_*.txt`）を**まとめて集計**し、弱点を数字で見られるようにする。本体（Arcaea側のMOD）ではなく、**ArcCreate（パソコン）側で行う**（本体側が煩雑になるため。ユーザーの決定）。

集計したい内容（ユーザーの要望）:

- **レーン別**（1・2・3・4）と、**左右別**（左手・右手）のミス数。
- **同時押しの扱い**: 例えば、レーン1と2が同時に来る場合、**2番は右手として数える**。つまり、左右は、レーンの番号ではなく、**同時に来るノーツの中での左右の位置**で決める（同時押しの2つは、左のほうを左手、右のほうを右手）。
- **ノーツの種類別**（タップ・ホールド・アークタップ・アーク）: アークでミスすることが多いのか、タップが多いのか。
- **曲のどの辺か**（時間帯別）。
- そのほか、思いつくものは追加していく（「夢が広がる」）。

設計上の注意:

- 同時押しの判定には、**ミスしていない相手のノーツ**も必要なので、集計は**譜面（AFF）を読んで**行う（ミス記録はミスした分しか持たない）。ミス記録の曲ID・難易度から譜面を探す必要があり、項目1の「曲フォルダの場所の設定」と共通の課題。
- ホールド・アークの粒ごとの複数の行は、**1本のノーツにまとめてから**数える（`MissLogMatcher`の`MissedNote`が使える）。
決定した規則（ユーザーの回答、2026-09-22）:

- **同時押しの2つ**は特別ルール: 左のほうを左手、右のほうを右手（例: レーン1と2が同時なら、1が左手、2が右手）。
- **単独のノーツ**は通常ルール: レーン1・2 = 左手、3・4 = 右手。
- **アーク**は色で決まっている: **赤いアーク = 右手、青いアーク = 左手**。
- **集計の範囲**: フォルダ内のミス記録を全部。
- **表示**: 画面内のどこかに集計を出す（書き出しは要らない）。

未決定・保留（**実装時は、案で進めて、ユーザーには聞かずに、実物を見てもらって直す**）:

- **アークタップ**の左右（色で決めるか、出てくる位置で決めるか）は難しい。ユーザーが後日考える。それまでは、左右に含めず、種類別（アークタップ）だけで数える。
- **3つ以上の同時押し**は、当面考えない（左端=左手、右端=右手、それ以外は「不明」として別に数える案）。

### 当面の優先外

- 再生速度変更時の音程維持は、現在の段階では実装しない。従来動作を維持する。
- 背景画像の細部は、見つからない場合に属性別標準背景へフォールバックできているため、優先度を上げない。

## 検証

研究用回帰試験は次で実行する。

```powershell
cd D:\chaos\MAGATU_EMULATOR\ArcCreate\Documentation\ArcaeaResearch
python -m unittest -v test_aff_judgement_analysis.py
```

ネイティブ解析の再実行には`tools\analyze_arcaea_native.py`を使う。LIEF 0.12.3とCapstone 5.0.7で確認済みである。APKと実物譜面は読み取り専用で扱い、生成物やキャッシュを原資料のディレクトリへ書き込まない。

実装変更では、対象機能だけを変更し、Unityでのビルドまたは該当テスト、同一AFFによる変更前後比較まで確認する。変更価値がある単位ごとにバージョンと変更履歴を更新し、コミットする。
