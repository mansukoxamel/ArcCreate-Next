# 変更履歴

## 0.9.2 - 2026-09-06

- Windows版のビルド前に個人用の外部`Skin`フォルダをGit対象外領域へ退避し、SHA-256で内容の一致を検証したうえで、ビルドの成否にかかわらず復元するようにした。
- 復元に失敗した場合はバックアップを残したままビルドエラーとして通知し、素材消失を隠さないようにした。
- 外部スキンの配置、正確なファイル名、再起動の必要性、現時点で反映されないパーティクル素材を`README.md`へ記録した。

## 0.9.1 - 2026-09-06

- 新規プロジェクト作成の内部処理は残したまま、編集画面上部の「新規」ボタンを非表示にした。

## 0.9.0 - 2026-09-06

- 編集画面の「開く」の横に読み込み履歴ボタンを追加した。
- 直接読み込みと旧プロジェクト読み込みの履歴を、判別しづらいAFF・OGG名ではなく曲フォルダのフルパスで最大10件保存・表示するようにした。
- 履歴から曲フォルダを選び、通常のAFF選択フローですぐ編集を再開できるようにした。

## 0.8.1 - 2026-09-06

- アーク初回取得のY範囲を本家5.5.8と同じ下側3.1・上側2.5へ修正し、保持中は従来どおり上下2.5を維持した。
- 本家5.5.8でも異色アークの近接判定が内部入力平面の距離200未満、全指受理猶予が500msのまま維持されていることを確認した。

## 0.8.0 - 2026-09-06

- tapとarctapの表示開始を、本家3.6.1と5.5.8に共通するZ=-90～-80の透明度変化へ修正した。
- arctapの最大不透明度を240/255へ修正し、通過後の後方フェードを削除した。
- 通常arcはZ=-95～-85、slamはZ=-90～-80で不透明度を75/255から225/255へ変化させる本家方式へ修正した。
- holdとarcの判定面Z=0での手前クリップが本家と一致することを確認した。

## 0.7.0 - 2026-09-06

- アークの初回取得を120ms先・X幅3.02、保持中を現在位置・X幅1.9として分離し、Y幅2.5を含む境界値を本家同様に範囲外とした。
- 指が範囲外へ動いた場合は所有を維持し、物理的に離した場合だけ`trunc(min(4I, 1000ms))`の再取得ロックを掛けるよう修正した。
- 通常判定点のMiss猶予を`min(2I, 500ms)`、接続部で方向が変わる後続アーク先頭だけを`min(I/2, 500ms)`へ修正した。
- 異色アークの近接判定を内部入力平面の距離200未満、全指受理猶予を500msへ変更した。

## 0.6.0 - 2026-09-06

- アークの接続条件を、本家Arcaea 3.6.1と同じ「時刻差9ms以下、X差0.1未満、Y完全一致、通常／トレース種別一致」へ変更した。
- 接続可否に色の一致は必要ない本家仕様を回帰試験で固定した。

## 0.5.0 - 2026-09-06

- timing区間ごとのBPM積分値とハイスピードから、本家Arcaea 3.6.1と同じ倍率でZ距離を計算するようにした。
- Z距離から時刻を求める逆変換も同じ本家準拠の倍率に統一し、プロジェクトの基準BPMへの依存を除いた。

## 0.4.2 - 2026-09-06

- 譜面固有のジャケット画像、共通ジャケット画像の順で選び、通常名の画像がない場合は`1080_`付きの高解像度画像を読み込むようにした。

## 0.4.1 - 2026-09-06

- Windows版のアプリアイコンを`LogoDesktop_Xynapse.png`へ変更。

## 0.4.0 - 2026-09-06

- Windows版でJPG画像または曲フォルダをドラッグ＆ドロップし、同じ場所のOGGとAFFから編集を開始できるようにした。
- 本編の曲データに合わせ、直接読み込みと画像選択の対応形式をJPEG（`.jpg`）に限定した。
- 同じ音源を使用する譜面へ切り替えた場合、音楽を止めずに現在位置と再生状態を維持して譜面だけを切り替えるようにした。
- 異なる音源を使用する譜面へ切り替えた場合、切り替え先の音源を0秒から自動再生するようにした。
- 編集中の譜面を切り替える際、未保存の変更がある場合だけ保存確認を表示するようにした。

## 0.3.3 - 2026-09-05

- Windows版でAFFまたはOGGをドラッグ＆ドロップし、`.arcproj`を作らずに譜面編集を開始できるようにした。
- AFFは同名OGGを優先し、なければ`base.ogg`を読み込むようにした。
- OGGに対応するAFFが複数ある場合は選択画面を表示し、AFFがない場合は`2.aff`を新規作成するようにした。
- 対応音声形式をArcaea本体で利用するOGGに限定し、`preview.ogg`は譜面用音声の対象外とした。

## 0.3.2 - 2026-09-05

- アーク描画の折れ線を、整数ミリ秒の区間長ではなく、本家Arcaeaと同じ継続時間基準の正規化時間で分割するよう修正。
- AFFのアーク端点を本家と同じ内部座標へ0方向に整数化してから曲線補間するよう修正。

## 0.3.1 - 2026-09-05

- Unityスプラッシュ画面を無効化し、旧バージョン向けの強制停止処理を削除。
- Windows版で未使用コードを除去しつつ、Lua・YAML・設定保存などのリフレクション対象を維持するよう設定。
- Windows版の収録シーンを編集に必要なBoot・Compose・Gameplayへ限定。その他のシーンはソースを残したままビルド対象外へ変更。
- Unity EditorからBootシーンを再生した場合も、案内画面ではなく編集画面へ直接進むよう変更。
- Windowsビルド容量を192.16MBから約136.3MBへ削減し、比較時の初回起動を約8.5秒から約5.1秒、通常起動を約2.6秒から約2.0秒へ短縮。

## 0.3.0 - 2026-09-05

- アプリの表示名を `ArcCreate Next`、Windows実行ファイル名を `ArcCreateNext.exe` に変更。
- 内部アプリIDは既存の設定データを引き継ぐため変更せず維持。

## 0.2.2 - 2026-09-05

- 起動時に毎回表示されていた案内画面を廃止し、通常の編集画面を直接表示するよう変更。
- 起動画面に含まれていたDiscord・Ko-fiへの誘導、言語切替、最近開いたプロジェクト一覧を起動時に実行しないよう変更。

## 0.2.1 - 2026-09-05

- Unity 6ビルド後にUI文字が白い矩形になる問題を、TextMesh Pro Essential Resourcesのシェーダー更新で修正。

## 0.2.0 - 2026-09-05

- 開発・ビルド環境をUnity 2022.3.62f3からUnity 6000.3.20f1（Unity 6.3 LTS）へ移行。
- Unity 6向けに公式パッケージとプロジェクト設定を更新。
- Unity 6で廃止予定となった検索、テキスト折返し、リフレッシュレートAPIを現行APIへ更新。
- 波形表示シェーダーのループ展開定数を整数型へ修正し、Unity 6のシェーダー警告を解消。
- 既に廃止された旧PostProcessing一式のシーン残存参照とプロファイルを削除。
- 実装と矛盾していたKeyChannelの範囲外テストを、端値へ固定する現行仕様に修正。

## 0.1.2 - 2026-09-05

- アーク追従tiltを、本家Arcaea 3.6.1と同じ1更新ごとの追従4%・中央復帰2%へ修正。

## 0.1.1 - 2026-09-05

- camera命令 `s` の補間をsmoothstepから本家Arcaea 3.6.1と同じ線形補間へ修正。
- 本家互換挙動を既定動作とし、互換モードを設けない開発方針を明文化。
